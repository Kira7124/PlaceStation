<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.project3.placestation.repository.interfaces.BizRepository">

	<update id="updateBizByBizId">
		update biz set
		biz_brand_name = #{biz.bizBrandName} ,
		biz_tel = #{biz.bizTel} 
		where biz_id = #{bizId}
	</update>
	
	<select id="findFortOneKeyByBizNo" resultType="com.project3.placestation.payment.model.dto.PaymentFortOneKeyDto">
		select imp_uid as impUid , imp_key as imp_key , imp_secret as impSecret 
		from biz
		where biz_no = #{bizNo}
	</select>
</mapper>